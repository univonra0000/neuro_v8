Use these Cloudflare Workers Builds settings:

Build command:
None

Deploy command:
npx wrangler deploy --compatibility-date 2026-09-20

Root directory:
/

If your Cloudflare project is connected to GitHub, commit the included wrangler.toml to the main branch before Retry build.

The previous error was:
Can't set compatibility date in the future: 2026-09-23

This package uses 2026-09-20 and the deploy command also explicitly supplies 2026-09-20.

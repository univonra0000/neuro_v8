CLOUDFLARE FIX — CURRENT ERROR

Your screenshot's current error is:
"Version of script does not export class 'NeuroSyncRoom'"

This package fixes that mismatch.

wrangler.toml uses:
  class_name = "NeuroRoom"

src/worker.js exports:
  export class NeuroRoom extends DurableObject

IMPORTANT:
Because Cloudflare is connected to GitHub, replace/commit these files into:
  univonra0000/neuro_v8
  branch: main

Then Retry deployment.

Workers Builds:
Build command: None
Deploy command: npx wrangler deploy
Root directory: /

The compatibility date is:
2026-09-20

Do NOT use NeuroSyncRoom in wrangler.toml.
